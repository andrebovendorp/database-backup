# Database Backup System Requirements
# Cross-platform database backup solution with Windows optimization

# Core dependencies (required)
requests>=2.31.0          # HTTP requests for Telegram notifications
PyYAML>=6.0              # YAML configuration file parsing

# Built-in modules used for Windows compatibility:
# - tempfile (TemporaryDirectory for Windows-safe temp handling)
# - shutil (make_archive/unpack_archive for cross-platform compression)
# - glob (database tool path detection on Windows)
# - subprocess (cross-platform command execution)

# Optional dependencies for enhanced functionality
# Uncomment if you need these features:
# psycopg2-binary>=2.9.0  # PostgreSQL adapter for advanced connection handling
# pymongo>=4.5.0          # MongoDB adapter for advanced features
# python-dotenv>=1.0.0    # Environment variable management from .env files
# click>=8.1.0            # Enhanced CLI interface with better help
# colorama>=0.4.6         # Colored terminal output (Windows compatible)
# tabulate>=0.9.0         # Table formatting for better reports

# External tools required (must be installed separately):
# - PostgreSQL client tools (pg_dump, psql) - https://www.postgresql.org/download/
# - MongoDB Database Tools (mongodump, mongorestore) - https://docs.mongodb.com/database-tools/

